services:
  - type: web
    name: sistema-matematica
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn flask_migration.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
